---
// Wordmark logo (placeholder — swap for a real asset when available).
interface Props {
  variant?: 'light' | 'dark';
  class?: string;
}
const { variant = 'dark', class: className = '' } = Astro.props;
const text = variant === 'light' ? 'text-white' : 'text-ink-900';
const sub = variant === 'light' ? 'text-white/55' : 'text-slatey-500';
---
<a href="/" class={`group inline-flex items-center gap-2.5 ${className}`} aria-label="Comstrat — home">
  <span class="grid h-9 w-9 place-items-center rounded-lg bg-ink-900 ring-1 ring-white/10 transition-transform group-hover:scale-105">
    <svg viewBox="0 0 64 64" class="h-5 w-5" aria-hidden="true">
      <path d="M36 12 L20 36 H30 L28 52 L46 26 H35 L36 12 Z" fill="#F97316" />
    </svg>
  </span>
  <span class="leading-none">
    <span class={`block font-display text-lg font-extrabold tracking-tight ${text}`}>COMSTRAT</span>
    <span class={`block text-[10px] font-medium uppercase tracking-[0.22em] ${sub}`}>Industrial Ltd</span>
  </span>
</a>
